# ============================================================
#  ROOT .env — docker-compose va deploy.ps1 uchun
#  Nusxa: Copy-Item .env.example .env
# ============================================================

# Deploy script shu nomdan foydalanadi (server path va arxiv nomi)
PROJECT_NAME=deep-hire

# ── MongoDB (docker-compose mongo servisi uchun) ─────────────
MONGO_USER=admin
MONGO_PASSWORD=changeme

# ── MinIO ────────────────────────────────────────────────────
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
